# DWH и Data Lakehouse

## Решение по горизонтам

| Горизонт | Основной подход | Назначение |
|---|---|---|
| 3 месяца | PostgreSQL DWH | • Общие измерения клиента, счёта и продукта<br><br>• Факты операций и датированные остатки<br><br>• Минимальные BI-витрины<br><br>• Сверенная управленческая отчётность |
| 12 месяцев | Гибрид PostgreSQL DWH + Iceberg Lakehouse | • DWH для стабильной отчётности<br><br>• Lakehouse для полной истории, событий и архивов<br><br>• Trino для SQL над Iceberg<br><br>• DataHub для общего governance и lineage |

PostgreSQL не заменяется Lakehouse. Каждый компонент получает собственный класс нагрузки, а данные переходят в DWH только после стандартизации и проверки.

## Разделение данных

| Данные | PostgreSQL DWH | Iceberg Lakehouse | Object Storage вне таблиц | Почему |
|---|---|---|---|---|
| Клиент | • Сертифицированное измерение<br><br>• SCD Type 2<br><br>• Разрешённые аналитические атрибуты | • Исходные версии FU/RB<br><br>• Match/merge history<br><br>• Детальный lineage | Подтверждающие файлы KYC | DWH нужен стабильный профиль. Lakehouse сохраняет историю разрешения конфликтов и позволяет пересборку |
| Счёт и договор | • Измерения<br><br>• Датированные снимки<br><br>• Связь с продуктом и клиентом | • CDC-история<br><br>• Все версии статусов и условий | Подписанные договорные файлы | Снимки удобны для отчётности, а полная история изменений объёмна |
| Продукт и справочники | • Использованные версии измерений | • Полная история RDM и Product Catalog<br><br>• Маппинг FU/RB | Экспорт утверждённых справочников | Отчёт должен воспроизводить версию кода на бизнес-дату |
| Карта | • Токенизированное измерение карты<br><br>• Агрегаты портфеля | • Детальные карточные события<br><br>• Авторизации<br><br>• Клиринг | Архивы платёжных файлов | Высокочастотная история не должна раздувать DWH. PAN не хранится вне PCI-контура |
| Кредит | • Портфельные снимки<br><br>• Остаток и просрочка<br><br>• Регуляторные показатели | • Версии графика<br><br>• События решения<br><br>• Полная история реструктуризаций | Документы заявки и договора | DWH оптимален для среза. Lakehouse нужен для истории и моделирования риска |
| Платёж | • Сверенный факт платежа<br><br>• Статус исполнения | • Полный жизненный цикл распоряжения<br><br>• Технические события | Платёжные файлы и manifest | DWH содержит бизнес-факт, Lakehouse сохраняет детальные шаги обработки |
| Операция | • `fact_transactions`<br><br>• Сверенные суммы<br><br>• Регуляторные агрегаты | • Неизменяемый поток всех доменов<br><br>• Сторно<br><br>• Поздние события | Архивные выгрузки | DWH хранит оптимизированную звезду, Lakehouse — полную гранулярность и replay |
| Обращение и заявка | • SLA и воронки<br><br>• Тематические агрегаты | • События статусов<br><br>• Обезличенный текст<br><br>• История решений | Вложения и исходные документы | Детальный текст и события нужны отдельным сценариям, но не регламентированной модели |
| Согласие | • Статус, действовавший на момент использования данных | • Неизменяемая история выдачи и отзыва | Доказательство получения | Необходимо доказать правовое основание для каждого периода |
| Контрагент | • Нормализованное измерение для платежной аналитики | • Снимок реквизитов платежа<br><br>• История entity resolution<br><br>• AML-связи | Внешние списки и выгрузки | Текущий реестр не должен перезаписывать исторические реквизиты факта |
| Документ | Только метаданные и бизнес-связи | Метаданные, извлечённые признаки и результаты обработки | Бинарное содержимое с versioning и retention lock | Файлы не хранятся в PostgreSQL и Iceberg как BLOB |

## Роль Apache Iceberg

Apache Iceberg задаёт открытый table format поверх object storage:

- атомарные коммиты и согласованные snapshots.
- schema evolution без ручного переписывания всей таблицы.
- partition evolution при изменении профиля запросов.
- time travel для воспроизводимости и расследований.
- hidden partitioning, уменьшающий зависимость запросов от физического layout.
- совместную работу batch и streaming-писателей через контролируемые commit-процессы.

Iceberg не решает качество, каталогизацию и безопасность автоматически. Эти обязанности остаются у пайплайнов, Data Quality Gate, IAM и DataHub.

## Роль Trino

Trino предоставляет SQL-доступ к Iceberg и curated data products:

- интерактивный анализ большой истории без загрузки всего объёма в PostgreSQL.
- федеративные запросы во время контролируемого перехода.
- доступ BI, Risk и Data Science через отдельные resource groups.
- единый SQL-интерфейс для доменных curated-наборов.

Trino не используется для транзакционных команд и не становится системой хранения. Тяжёлые запросы ограничиваются квотами, timeouts, concurrency limits и workload management.

## Физическая организация Lakehouse

| Зона | Формат | Разбиение | Retention | Правило публикации |
|---|---|---|---|---|
| `raw` | Iceberg / Parquet с исходным payload | • `source_system`<br><br>• `ingestion_date` | По классу данных и правовым требованиям | После технической проверки доставки и хеша |
| `standardized` | Iceberg / Parquet с канонической схемой | • `source_domain`<br><br>• `business_date` | Полная требуемая история | После schema, validity и reference checks |
| `curated` | Iceberg / Parquet по data product | • Домен<br><br>• Бизнес-дата<br><br>• Оптимизированный partition key | По контракту data product | После доменных проверок и reconciliation |
| PostgreSQL DWH | Реляционные fact/dimension | • Дата для крупных фактов<br><br>• Индексы по surrogate key | Регламентированный период | После Publication Gate и сертификации batch |

Конкретное partitioning выбирается по измеренным объёмам и запросам. Высокая кардинальность `customer_id` или `transaction_id` не используется как прямой partition key.

## Загрузка в PostgreSQL DWH

1. Airflow фиксирует snapshot curated-таблиц и `batch_id`.
2. Trino или выделенный ELT-job формирует набор для загрузки.
3. Stage-таблицы принимают данные без публикации пользователям.
4. Проверки сравнивают record count, суммы, валюты и ссылки на измерения.
5. SCD-измерения обновляются по business key и периоду действия.
6. Факты загружаются идемпотентно по source key.
7. Успешный batch атомарно переводит витрины в статус `certified`.
8. DataHub получает lineage от Iceberg snapshot до DWH-таблицы и BI-витрины.

## Ограничения и меры

| Ограничение | Риск | Мера |
|---|---|---|
| PostgreSQL DWH имеет ограничение вертикального масштаба | Рост истории замедляет загрузку и запросы | • Оставлять детальную историю в Lakehouse<br><br>• Партиционировать крупные факты<br><br>• Использовать агрегаты и materialized views<br><br>• Архивировать закрытые периоды |
| Small files в Lakehouse | Медленное планирование и чтение | • Compaction по расписанию<br><br>• Контроль размера файлов<br><br>• Ограничение частоты commits<br><br>• Мониторинг количества файлов |
| Поздние и изменённые события | Расхождение DWH и истории | • Watermark<br><br>• Допустимое окно lateness<br><br>• Пересчёт затронутых партиций<br><br>• Версионирование факта |
| Schema evolution источников | Поломка consumers | • Schema Registry<br><br>• Compatibility checks<br><br>• Контрактные тесты<br><br>• Параллельная версия |
| Федеративные запросы Trino | Непредсказуемая нагрузка | • Resource groups<br><br>• Query limits<br><br>• Отдельные compute pools<br><br>• Предрасчёт частых наборов |
| Дублирование данных между Lakehouse и DWH | Неясный источник аналитической истины | • Назначить DWH сертифицированным источником отчётности<br><br>• Curated считать детальным продуктовым слоем<br><br>• Публиковать статус и lineage в DataHub |
| Чувствительные данные в raw | Расширенный риск доступа | • Отдельные зоны и ключи<br><br>• ABAC<br><br>• Токенизация<br><br>• Запрет чувствительных значений в каталоге и логах |

## Критерий размещения нового набора

Набор помещается в DWH, если имеет стабильную схему, требуется для повторяемой отчётности и укладывается в контролируемый объём. Набор помещается в Lakehouse, если требуется полная история, высокая гранулярность, schema evolution, повторная обработка или data science. Один набор может существовать в обоих слоях, если curated Iceberg является детальным источником, а DWH — сертифицированной проекцией с явным lineage.

## Архитектурный запас на 3–5 лет

Целевая платформа на 12 месяцев не требует немедленного внедрения отдельного MPP DWH или нескольких Lakehouse-кластеров. Она должна сохранять возможность такого развития без изменения канонической модели и data contracts.

| Область | Базовое состояние на 12 месяцев | Опциональное развитие при росте |
|---|---|---|
| Certified DWH | PostgreSQL хранит сертифицированные факты, измерения и витрины | • Read replicas для BI<br><br>• Rolling hot window<br><br>• Агрегаты вместо полной детализации<br><br>• Columnar/MPP при подтверждённом пределе |
| Историческая детализация | Iceberg curated хранит полную историю | • Разделение compute pools по доменам<br><br>• Monthly cold partitions<br><br>• Предрасчёт популярных data products |
| Object Storage | Raw, curated, документы и архивы используют общий lifecycle по классам | • Hot/warm/cold tiers<br><br>• Раздельные buckets и ключи по доменам<br><br>• Legal hold registry |
| Trino | Resource groups разделяют BI, Risk и Data Science | • Независимые coordinator/worker pools<br><br>• Autoscaling<br><br>• Query routing и materialized datasets |
| Iceberg Catalog | Единый отказоустойчивый catalog управляет таблицами | • Репликация metadata<br><br>• Разделение namespaces<br><br>• Отдельные catalogs по зонам доверия при необходимости |

## Hot/cold политика

1. PostgreSQL хранит период, который регулярно нужен BI и отчётности.
2. Полная история остаётся в Iceberg curated и доступна через Trino.
3. Закрытый период удаляется из PostgreSQL только после проверки Iceberg snapshot, lineage и возможности воспроизведения.
4. Представление или semantic layer скрывает от consumer физическую границу hot/cold.
5. Regulatory snapshot хранит идентификаторы версии DWH batch, Iceberg snapshot и справочников.
6. Legal hold блокирует lifecycle независимо от стоимости.

Длительность hot window не фиксируется заранее. Она выбирается по профилю запросов, срокам отчётности, стоимости и результатам нагрузочного тестирования.

## SLO обслуживания Iceberg

| Метрика | Нормальное состояние | Триггер | Действие |
|---|---|---|---|
| Files per table | Стабильно около post-compaction baseline | Рост более чем в два раза | • Внеплановая compaction<br><br>• Проверка micro-batch window<br><br>• Ограничение commit frequency |
| Average file size | В целевом диапазоне, определённом нагрузочным тестом | Устойчивое снижение ниже диапазона | • Увеличить batch<br><br>• Объединить writers<br><br>• Настроить adaptive flush |
| Compaction lag | Maintenance завершается до критических запросов | Job не укладывается в maintenance window | • Выделить compute pool<br><br>• Приоритизировать hot partitions<br><br>• Пересмотреть partitioning |
| Snapshot count | Соответствует retention policy | Метаданные растут быстрее объёма данных | • Snapshot expiration<br><br>• Orphan file cleanup<br><br>• Проверка зависимых tags/branches |
| Trino planning p95 | Стабилен при сопоставимом scan volume | Растёт быстрее объёма чтения | • Проверить manifests и file count<br><br>• Compaction<br><br>• Partition evolution |

## Workload isolation

- ежедневная сертификация DWH получает гарантированный Airflow pool и compute quota.
- backfill и архивные загрузки используют отдельный pool и могут быть приостановлены.
- Trino разделяет regulatory, BI, Risk и ad-hoc через resource groups.
- compaction не конкурирует с Publication Gate за один и тот же лимит compute.
- карточные события могут получить отдельные Kafka topics и streaming workers.
- FinOps измеряет стоимость по data product и домену.

## Решение о структурном масштабировании

Columnar/MPP или облачный warehouse рассматривается, если одновременно подтверждены:

- PostgreSQL после hot/cold и read replicas не укладывается в batch window.
- p95 критических BI-запросов превышает SLO.
- вертикальное масштабирование не даёт требуемого резерва или экономически нецелесообразно.
- эксплуатационная команда готова поддерживать новый certified-слой.
- пилот подтверждает воспроизводимость сверки, SCD, security и lineage.

Если условия не выполнены, PostgreSQL сохраняется. Горизонт 3–5 лет является периодом наблюдения и принятия решений, а не обязательным сроком замены.
