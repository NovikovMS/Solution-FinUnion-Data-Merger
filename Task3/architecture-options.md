# Сравнение архитектурных вариантов

## Критерии

Варианты оцениваются по скорости запуска, финансовой корректности, масштабу истории, поддержке событий, управляемости, стоимости эксплуатации и соответствию организационной зрелости объединённого банка.

## Сравнение

| Подход | Плюсы | Минусы | Когда подходит | Применимость к FinUnion + RetailBank |
|---|---|---|---|---|
| DWH на PostgreSQL | • Знакомая реляционная модель и SQL<br><br>• ACID и строгие ограничения<br><br>• Быстрый запуск небольших витрин<br><br>• Простая интеграция с BI<br><br>• Невысокий порог эксплуатации | • Ограниченное горизонтальное масштабирование<br><br>• Дорого хранить детальную многолетнюю историю<br><br>• Неудобен для полуструктурированных событий и файлов<br><br>• Частые schema changes усложняют загрузку<br><br>• Конкуренция тяжёлых запросов за ресурсы | • Регламентированные наборы<br><br>• Умеренные объёмы<br><br>• Стабильная схема<br><br>• Быстрый промежуточный результат | Высокая применимость для первых общих витрин и отчётности.<br><br>Недостаточен как единственная целевая платформа для карточных событий, архивов и полной истории |
| Data Lakehouse | • Масштабируемое хранение в object storage<br><br>• Структурированные и полуструктурированные данные<br><br>• Разделение хранения и вычислений<br><br>• Time travel и schema evolution через Iceberg<br><br>• Повторная обработка из raw<br><br>• Поддержка batch и streaming | • Больше компонентов и эксплуатационных навыков<br><br>• Требуются compaction, partition management и контроль small files<br><br>• Качество не появляется автоматически<br><br>• Интерактивные запросы зависят от оптимизации таблиц<br><br>• Регуляторная модель требует отдельного стабильного слоя | • Большая история<br><br>• Высокочастотные события<br><br>• Data Science и Anti-fraud<br><br>• Несколько форматов и источников | Высокая применимость для объединённой истории FU/RB, CDC, карточных событий, документов и повторной обработки.<br><br>Для обязательной отчётности нужен сертифицированный слой и жёсткий publication gate |
| Data Mesh | • Явное доменное владение<br><br>• Data as a Product<br><br>• Масштабирование ответственности между командами<br><br>• Контракты, SLA и качество рядом с producer<br><br>• Соответствует BIAN-границам | • Не является готовой технологией хранения<br><br>• Требует зрелых доменных команд и federated governance<br><br>• Риск дублирования платформ и терминов<br><br>• Высокая организационная стоимость<br><br>• Нельзя внедрить только покупкой инструмента | • Много зрелых автономных доменов<br><br>• Есть единая self-service платформа<br><br>• Владельцы готовы отвечать за SLA data products | Применим как набор принципов для Customer, Accounts, Payments, Cards и Loans.<br><br>Полноценная децентрализация преждевременна до унификации терминов, качества и платформы |

## Дополнительные варианты

| Вариант | Почему не выбран основным |
|---|---|
| Только Data Lake без table format | Не обеспечивает достаточную управляемость схем, ACID, time travel и воспроизводимость таблиц |
| Один корпоративный DWH для всех данных | Слишком дорог для сырых событий и архивов. Создаёт жёсткую зависимость всех сценариев от одной модели |
| Отдельная платформа в каждом домене | Увеличивает стоимость, усложняет безопасность и воспроизводит разрозненность FU/RB |
| Прямой BI над OLTP | Нарушает разделение нагрузок и делает клиентские сервисы зависимыми от аналитических запросов |

## Решение

Выбран **гибрид DWH + Lakehouse с принципами Data Mesh**.

### PostgreSQL DWH

Используется для стабильных фактов, измерений, сертифицированных витрин, управленческой и регуляторной отчётности. Он обеспечивает понятную SQL-модель и быстрый результат для контролируемого объёма.

### Data Lakehouse

Используется для raw-истории, CDC, событий, архивных выгрузок, документов и больших аналитических наборов. Apache Iceberg обеспечивает табличную семантику, а Trino предоставляет SQL-доступ.

### Data Mesh

Используется как операционная модель:

- домен владеет смыслом и качеством data product.
- общая платформа предоставляет ingestion, storage, compute, каталог и безопасность.
- federated governance задаёт обязательные контракты, идентификаторы, классификацию и lineage.
- физическая инфраструктура остаётся общей, чтобы не дублировать платформу в каждом домене.

## Почему гибрид подходит

1. PostgreSQL DWH позволяет быстро собрать согласованные витрины клиентов, счетов, продуктов и операций.
2. Lakehouse снимает ограничения по истории и событиям без переноса аналитической нагрузки в OLTP.
3. Raw-слой сохраняет исходные записи FU/RB и делает преобразования воспроизводимыми.
4. DWH получает только сертифицированные данные и остаётся стабильным для отчётности.
5. Доменные data products согласуются с BIAN и назначенными Data Owners.
6. Компоненты можно вводить поэтапно без остановки клиентских сервисов.

## Условия успеха

- единые `customer_id`, `product_id` и таблицы source links.
- Schema Registry и совместимые data contracts.
- контроль качества до публикации.
- автоматический lineage в DataHub.
- эксплуатационные владельцы Kafka, Airflow, Iceberg, Trino и PostgreSQL.
- доменные Owners и Stewards с измеримыми SLA.
- разграничение ресурсов интерактивной аналитики, batch и регуляторных расчётов.
